# Used only by the backend's evidence-grounded explanation endpoint.
# Never expose this value to the frontend or commit a populated .env file.
OPENAI_API_KEY=
# Optional. Defaults to gpt-5.
OPENAI_MODEL=gpt-5
